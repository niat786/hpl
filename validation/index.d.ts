export declare function isValidEmail(email: string, rules?: string[]): {
    message: string;
    type: string;
    code: string;
};
export declare function isValidPassword(password: string, rules?: string[]): {
    message: string;
    type: string;
    code: string;
};
export declare function isValidURL(url: string, rules?: string[]): {
    message: string;
    type: string;
    code: string;
};
